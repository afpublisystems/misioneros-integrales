{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "Docker (app + db + phpmyadmin)",
      "runtimeExecutable": "docker",
      "runtimeArgs": ["compose", "up", "--build"],
      "port": 8080
    },
    {
      "name": "Docker sin rebuild",
      "runtimeExecutable": "docker",
      "runtimeArgs": ["compose", "up"],
      "port": 8080
    }
  ]
}
